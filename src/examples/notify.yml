description: >
  Sample example description.
# Provide a use-case based example for using this orb.
# Everything in the `usage` section will be displayed in the orb registry.
# Comments are not retained.
usage:
  version: 2.1

  orbs:
    flaky-tests-notify: tadashi/flaky-tests-notify@1.0.0

  # This parameter is for passing from Scheduled pipelines
  # https://circleci.com/docs/scheduled-pipelines
  parameters:
    notify_flaky_tests:
      type: boolean
      default: false

  workflows:
    notify_flaky_tests:
      when: << pipeline.parameters.notify_flaky_tests >>
      jobs:
        - flaky-tests-notify/notify
